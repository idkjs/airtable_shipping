open Util
open Airtable

// for now this is how I look at the generated code
// it's ugly ok i get is
@react.component
let make = () => {
  <pre style={ReactDOM.Style.make(~backgroundColor="lightblue", ())}> {s(`


  
  `)} </pre>
}
